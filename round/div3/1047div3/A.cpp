#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

void solve() {
  ll k, x;
  cin >> k >> x;

  cout << (x << k) << "\n";
}

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);

  int t;
  cin >> t;

  while (t--) {
    solve();
  }
  return 0;
}
